import { createMuiTheme } from "@material-ui/core/styles";

const theme = {
  palette: {
    primary: { main: "#535353" },
    secondary: { main: "#03afff" },
    type: "dark"
  }
};

export default createMuiTheme(theme);
